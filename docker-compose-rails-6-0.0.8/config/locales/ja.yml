ja:
  activerecord:
    errors:
      models:
        post:
          attributes:
            base:
              content_attachment_byte_size_is_too_big: '添付ファイルは%{max_content_attachment_mega_bytes}MB以下にしてください。現在のファイルサイズ：%{bytes}/最大ファイルサイズ%{max_bytes}'
            title:
              blank:    'が空です。'
              too_long: 'が%{count}文字を超えています。'
            content:
              too_long: 'が%{max_content_length}文字を超えています。現在の文字数：%{current_content_length}/最大文字数:%{max_content_length}'
    attributes:
      post:
        title:   'タイトル'
        content: 'コンテンツ'
